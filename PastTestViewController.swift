//
//  PastTestViewController.swift
//  GUITestingNotify
//
//  Created by Chens on 30/10/2018.
//  Copyright © 2018 Chens. All rights reserved.
//

import UIKit

class PastTestViewController: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
        
        // MARK: segue swipe left in VDL
    
//        let leftSwipe = UISwipeGestureRecognizer(target: self, action: #selector(handleSwipe(sender:)))
//
//        view.addGestureRecognizer(leftSwipe)

        // end of segue swipe left in VDL
    }
    
    // MARK: segue swipe left

//    @objc func handleSwipe(sender: UISwipeGestureRecognizer) {
//        if sender.state == .ended {
//            performSegue(withIdentifier: "goLeft", sender: self)
//        }
//    }
    
    // end of segue swipe left
    

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
